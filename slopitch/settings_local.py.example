# Local settings for production deployment
# Copy this file to settings_local.py and update with your production values

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'your-super-secret-production-key-here'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = False

ALLOWED_HOSTS = [
    'your-domain.com',
    'www.your-domain.com',
    'your-ec2-ip-address',
    # Add your production domains/IPs here
]

# Database (if using something other than SQLite in production)
# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.postgresql',
#         'NAME': 'slopitch_db',
#         'USER': 'slopitch_user',
#         'PASSWORD': 'your_db_password',
#         'HOST': 'localhost',
#         'PORT': '5432',
#     }
# }

# Static files (production)
# STATIC_ROOT = '/var/www/your-site/static/'

# Media files (production)
# MEDIA_ROOT = '/var/www/your-site/media/'

# Email settings (if needed)
# EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
# EMAIL_HOST = 'smtp.your-provider.com'
# EMAIL_PORT = 587
# EMAIL_USE_TLS = True
# EMAIL_HOST_USER = 'your-email@domain.com'
# EMAIL_HOST_PASSWORD = 'your-email-password'

# Logging (optional)
# LOGGING = {
#     'version': 1,
#     'disable_existing_loggers': False,
#     'handlers': {
#         'file': {
#             'level': 'INFO',
#             'class': 'logging.FileHandler',
#             'filename': '/var/log/django/slopitch.log',
#         },
#     },
#     'loggers': {
#         'django': {
#             'handlers': ['file'],
#             'level': 'INFO',
#             'propagate': True,
#         },
#     },
# }